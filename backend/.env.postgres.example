# PostgreSQL connection for local development.
DATABASE_URL=postgresql+psycopg://privy:privy@localhost:5432/privy

# Optional: path to the legacy SQLite DB used by migrate_sqlite_to_postgres.py
# LEGACY_SQLITE_PATH=app/mapping_store.db
